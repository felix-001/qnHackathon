FROM alpine:latest

RUN apk --no-cache add \
    bash \
    curl \
    jq \
    supervisor \
    coreutils

WORKDIR /app

COPY scripts/bin-proxy.sh /app/bin-proxy
COPY scripts/bin-inspector.sh /app/bin-inspector
COPY scripts/bin-manifests.json /app/bin-manifests.json

RUN chmod +x /app/bin-proxy /app/bin-inspector

RUN echo '* * * * * /app/bin-proxy >> /app/bin-proxy.log 2>&1' > /etc/crontabs/root && \
    echo '* * * * * /app/bin-inspector >> /app/bin-inspector.log 2>&1' >> /etc/crontabs/root

CMD ["crond", "-f", "-l", "2"]