    server {
        listen 8090;
        location /files {
            root /usr/share/nginx/html;
            autoindex on;  # 允许查看目录文件列表
            autoindex_exact_size off;
            autoindex_localtime on;
        }
    }
